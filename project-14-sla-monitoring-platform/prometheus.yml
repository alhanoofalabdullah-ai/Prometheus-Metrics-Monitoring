global:

  scrape_interval: 15s

rule_files:

  - "/etc/prometheus/rules/sla-alert-rules.yml"

scrape_configs:

  - job_name: "business-services"

    static_configs:

      - targets:
          - "application01:8080"
          - "application02:8080"
          - "application03:8080"
