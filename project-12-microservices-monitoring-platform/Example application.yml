server:
  port: 8081

spring:
  application:
    name: customer-service

management:
  endpoints:
    web:
      exposure:
        include: "*"

  prometheus:
    metrics:
      export:
        enabled: true
