global:

  scrape_interval: 15s

rule_files:

  - "/etc/prometheus/rules/api-alert-rules.yml"

scrape_configs:

  - job_name: "api-monitoring"

    metrics_path: "/actuator/prometheus"

    static_configs:

      - targets:
          - "host.docker.internal:8080"
